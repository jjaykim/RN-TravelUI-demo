import React, { FunctionComponent } from 'react';
import { Text, View } from 'react-native';

interface LikedProps {}

export const Liked: FunctionComponent<LikedProps> = ({}) => {
  return (
    <View>
      <Text>Liked</Text>
    </View>
  );
};
